# Sessions

Generated transcripts from `/mock-interview` runs land here as
`YYYY-MM-DD-<topic>.md`. This folder's contents (other than this file) are
gitignored by default — see the root `.gitignore` and
[`../README.md`](../README.md#sessions) for how to keep a specific one.
